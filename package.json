{
  "name": "zero-human-sandbox-two",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Express backend with a health-check endpoint.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
