{
  "name": "zero-human-sandbox-two",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Express backend with health-check and ping endpoints.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --check server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
